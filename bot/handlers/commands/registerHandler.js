// Registration is now owned by Spartan. Keep /utopia register as a compatibility alias
// so there is exactly one access-granting registration path.
module.exports = require('./spartanRegisterHandler');
