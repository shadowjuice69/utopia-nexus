import {useEffect,useState}from"react";import{supabase}from"../services/supabase";import{loadNexusConfig,getNexusConfig}from"../services/nexusConfig";
export default function AttackLog(){const[attacks,setAttacks]=useState([]),[kd,setKd]=useState(""),[loading,setLoading]=useState(true),[filter,setFilter]=useState("all"),[search,setSearch]=useState("");useEffect(()=>{let live=true;async function load(){const c=await loadNexusConfig(true),k=c?.kd||getNexusConfig().kd||"";if(!k){if(live)setLoading(false);return}const{data}=await supabase.from("intel7_events").select("*").eq("kd_code",k).eq("channel_type","attacks").order("timestamp",{ascending:false}).limit(200);if(live){setKd(k);setAttacks(data||[]);setLoading(false)}}load();const iv=setInterval(load,30000);return()=>{live=false;clearInterval(iv)}},[]);const outgoing=a=>a.attacker_kingdom===kd||a.data?.direction==="outgoing",incoming=a=>a.target_kingdom===kd&&a.attacker_kingdom!==kd;const shown=attacks.filter(a=>(filter==="outgoing"?!outgoing(a):filter==="incoming"?!incoming(a):true)&&(!search||JSON.stringify(a).toLowerCase().includes(search.toLowerCase())));if(loading)return <div className="loading">⏳ Loading current Attack Log...</div>;return <div className="intel-panel"><div className="panel"><div style={{display:"flex",justifyContent:"space-between",gap:8,flexWrap:"wrap"}}><h2>⚔️ Attack Log · {kd}</h2><span>{attacks.length} current records · refresh 30s</span></div><div style={{display:"flex",gap:8,marginBottom:12}}>{["all","outgoing","incoming"].map(f=><button key={f} onClick={()=>setFilter(f)}>{f}</button>)}<input placeholder="🔍 Search every field..." value={search} onChange={e=>setSearch(e.target.value)} style={{flex:1}}/></div>{shown.length?shown.map(a=><details key={a.id} style={{padding:10,marginBottom:6,border:"1px solid rgba(255,255,255,.08)",borderRadius:8}}><summary><b>{a.event_type||"attack"}</b> · {a.attacker_province||"?"} ({a.attacker_kingdom||"?"}) → {a.target_province||"?"} ({a.target_kingdom||"?"}) · {a.timestamp?new Date(a.timestamp).toLocaleString():"—"}</summary><pre style={{whiteSpace:"pre-wrap",fontSize:11}}>{JSON.stringify(a,null,2)}</pre></details>):<p>No current attacks recorded.</p>}</div></div>}
